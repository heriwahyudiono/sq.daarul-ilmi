<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="bg-white shadow-md rounded-lg p-8 w-96">
    <h2 class="text-2xl font-semibold mb-4">Login</h2>

    <% if (typeof successMessage !== 'undefined') { %>
      <p class="text-green-600 mb-4"><%= successMessage %></p>
    <% } %>

    <form action="/login" method="POST">
      <label class="block text-sm mb-1" for="email">Email:</label>
      <input class="w-full px-3 py-2 border rounded-md focus:ring focus:ring-blue-300" type="email" id="email" name="email" required>
      
      <label class="block text-sm mt-3 mb-1" for="password">Password:</label>
      <input class="w-full px-3 py-2 border rounded-md focus:ring focus:ring-blue-300" type="password" id="password" name="password" required>
      
      <% if (typeof errorMessage !== 'undefined') { %>
        <p class="text-red-600 mt-2"><%= errorMessage %></p>
      <% } %>
      
      <button class="w-full bg-blue-500 text-white py-2 mt-4 rounded-md hover:bg-blue-600 transition duration-300" type="submit">Login</button>
    </form>
    
    <div class="mt-4 text-right">
      <a class="text-blue-500 hover:underline" href="/forgot-password">Lupa password?</a>
    </div>
    
    <div class="mt-4 text-center">
      <p class="text-gray-600">Belum punya akun? <a class="text-blue-500 hover:underline" href="/register">Daftar</a></p>
    </div>
  </div>
</body>
</html>
