<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Profil</title>
</head>
<body>

  <% if (user.id) { %>
    <a href="/user?id=<%= user.id %>">Kembali</a><br />
  <% } %>

  <h2>Edit Profil</h2>

  <form action="/update-user" method="POST">
    <label for="nama_lengkap">Nama Lengkap:</label>
    <input type="text" id="nama_lengkap" name="nama_lengkap" value="<%= user.nama_lengkap %>" /><br />

    <label for="jenis_kelamin">Jenis Kelamin:</label>
    <select id="jenis_kelamin" name="jenis_kelamin">
      <option value="Laki-laki" <%= user.jenis_kelamin === 'Laki-laki' ? 'selected' : '' %>>Laki-laki</option>
      <option value="Perempuan" <%= user.jenis_kelamin === 'Perempuan' ? 'selected' : '' %>>Perempuan</option>
    </select><br />

    <label for="tanggal_lahir">Tanggal Lahir:</label>
    <input type="date" id="tanggal_lahir" name="tanggal_lahir" value="<%= user.tanggal_lahir %>" /><br />

    <label for="email">Email:</label>
    <input type="text" id="email" name="email" value="<%= user.email %>" /><br />

    <label for="nomor_telepon">Nomor Telepon:</label>
    <input type="text" id="nomor_telepon" name="nomor_telepon" value="<%= user.nomor_telepon %>" /><br />

    <input type="submit" value="Simpan" />
  </form>
</body>
</html>
